using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WallCharger : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //this is wall charger script
    }

    // Update is called once per frame
    void Update()
    {
        //test foreal
    }
}
